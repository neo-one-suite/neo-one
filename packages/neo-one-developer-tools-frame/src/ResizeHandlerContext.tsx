import * as React from 'react';
import { ResizeHandler } from './ResizeHandler';

// tslint:disable-next-line no-any
export const ResizeHandlerContext: any = React.createContext<ResizeHandler>(undefined as any);
