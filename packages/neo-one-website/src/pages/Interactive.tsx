import * as React from 'react';
import { InteractiveApp } from '../components';
import { InteractiveLayout } from '../layout';

export const Interactive = () => (
  <InteractiveLayout>
    <InteractiveApp />
  </InteractiveLayout>
);
