import * as React from 'react';
import { EditorContextType } from './types';

// tslint:disable-next-line no-any
export const EditorContext: any = React.createContext<EditorContextType>(undefined as any);
