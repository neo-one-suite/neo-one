// tslint:disable no-any
import {
  ifProp as ifPropInternal,
  prop as propInternal,
  switchProp as switchPropInternal,
  withProp as withPropInternal,
} from 'styled-tools';

export const prop: any = propInternal;
export const ifProp: any = ifPropInternal;
export const switchProp: any = switchPropInternal;
export const withProp: any = withPropInternal;
