import { GetCompletionBaseHelper } from './GetCompletionBaseHelper';

// Input: [completion]
// Output: [errorVal]
export class GetCompletionErrorHelper extends GetCompletionBaseHelper {
  protected readonly index = 1;
}
