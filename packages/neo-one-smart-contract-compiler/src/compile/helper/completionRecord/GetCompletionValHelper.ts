import { GetCompletionBaseHelper } from './GetCompletionBaseHelper';

// Input: [completion]
// Output: [errorVal]
export class GetCompletionValHelper extends GetCompletionBaseHelper {
  protected readonly index = 0;
}
