export enum DiagnosticCode {
  UNSUPPORTED_SYNTAX = 0,
  UNKNOWN_TYPE,
  EXPECTED_NUMBER,
  EXPECTED_STRING,
  REFERENCE_ERROR,
  SOMETHING_WENT_WRONG,
  EXPECTED_PROPERTY,
  EXPECTED_CAST,
  TRANSPILATION_ERROR,
  INVALID_CONTRACT_PARAMETER,
  INVALID_CONTRACT_TYPE,
  INVALID_CONTRACT_METHOD,
  INVALID_CONTRACT_EVENT,
  INVALID_VALUE_TYPE,
  INVALID_SYS_CALL,
  UNKNOWN_SYMBOL,
}
