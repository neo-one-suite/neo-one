codecov:
  ci:
    - '!appveyor'

ignore:
  - 'common'
  - 'scripts'
  - '**/__e2e__/**'
  - '**/__data__/**'

comment:
  layout: 'reach, diff, flags, files'

coverage:
  status:
    project:
      default:
        threshold: 0.30
