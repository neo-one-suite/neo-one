codecov:
  ci:
    - "!appveyor"

ignore:
  - "jest"
  - "scripts"

fixes:
  - "dist/neo-one::"

comment:
  layout: "reach, diff, flags, files"

coverage:
  status:
    project:
      default:
        threshold: 0.30
